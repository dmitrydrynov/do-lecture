{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "**/.env"
  ],
  "globalEnv": [
    "APP_NAME",
    "JWT_SECRET",
    "NODE_ENV",
    "AIRTABLE_API_KEY",
    "AIRTABLE_BASE_ID",
    "NEXT_PUBLIC_APP_URL",
    "NEXT_PUBLIC_TON_TESTNET_APIKEY",
    "NEXT_PUBLIC_TON_MAINNET_APIKEY",
    "API_SECRET_KEY",
    "CRYPTOBOT_TOKEN"
  ],
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**",
        ".next/**"
      ]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}